# راهنمای اجرای رابط وب GraphRAG

## 🚀 نحوه اجرا

### 1. فعال‌سازی محیط مجازی
```bash
.\graphrag_env\Scripts\activate
```

### 2. اجرای برنامه وب
```bash
python web_app.py
```

### 3. دسترسی به رابط وب
در مرورگر خود به آدرس زیر بروید:
```
http://localhost:5000
```

## 📋 قابلیت‌های رابط وب

### 🔍 بخش سوال
- **ورود سوال**: می‌توانید سوال خود را به انگلیسی وارد کنید
- **انتخاب روش بازیابی**: 
  - BFS (جستجوی سطح اول)
  - DFS (جستجوی عمیق اول)
  - کوتاه‌ترین مسیر
  - همسایه‌ها
  - ترکیبی
- **انتخاب مدل تولید متن**:
  - GPT Simulation
  - Simple Template
  - Custom Model
- **تنظیم عمق جستجو**: از 1 تا 5

### 📊 نمایش نتایج

#### 1. **مراحل پردازش**
- نمایش تمام مراحلی که سیستم طی کرده است

#### 2. **کلمات کلیدی استخراج شده**
- نمایش کلمات کلیدی که از سوال استخراج شده‌اند

#### 3. **نودهای تطبیق یافته**
- نمایش نودهایی که با کلمات کلیدی تطبیق یافته‌اند

#### 4. **اطلاعات بازیابی شده**
- **تب نودها**: نمایش تمام نودهای بازیابی شده
- **تب یال‌ها**: نمایش روابط بین نودها
- **تب مسیرها**: نمایش مسیرهای یافت شده
- **تب متن زمینه**: نمایش متنی که به مدل تولید داده شده

#### 5. **پاسخ تولید شده**
- نمایش پاسخ نهایی با درصد اطمینان

### 📈 اطلاعات گراف
- نمایش آمار کلی گراف
- نمودار دایره‌ای انواع نودها

## 🎯 مثال‌های سوال

1. **What is the relationship between HMGB3 and diabetes?**
2. **What drugs treat diabetes?**
3. **What genes are expressed in the heart?**
4. **How does TP53 relate to cancer?**
5. **What biological processes does BRCA1 regulate?**

## 🔧 تنظیمات پیشرفته

### تغییر پورت
در فایل `web_app.py` خط زیر را تغییر دهید:
```python
app.run(debug=True, host='0.0.0.0', port=5000)
```

### تغییر آدرس
برای دسترسی از سایر دستگاه‌ها:
```python
app.run(debug=True, host='0.0.0.0', port=5000)
```

## 🛠️ عیب‌یابی

### مشکل در بارگذاری مدل spaCy
```bash
python -m spacy download en_core_web_sm
```

### مشکل در نصب کتابخانه‌ها
```bash
pip install -r requirements.txt
```

### مشکل در دسترسی به پورت
پورت 5000 را در فایروال باز کنید یا پورت دیگری استفاده کنید.

## 📁 ساختار فایل‌ها

```
tir/
├── web_app.py              # برنامه اصلی Flask
├── graphrag_service.py     # سرویس GraphRAG
├── templates/
│   └── index.html         # صفحه اصلی
├── static/
│   ├── css/
│   │   └── style.css      # استایل‌ها
│   └── js/
│       └── app.js         # کد JavaScript
└── RUN_WEB_APP.md         # این فایل
```

## 🎨 ویژگی‌های رابط کاربری

- **طراحی ریسپانسیو**: سازگار با تمام دستگاه‌ها
- **رابط فارسی**: پشتیبانی کامل از زبان فارسی
- **انیمیشن‌ها**: انیمیشن‌های نرم و زیبا
- **رنگ‌بندی مدرن**: استفاده از گرادیان‌های زیبا
- **نمایش مرحله‌ای**: نمایش کامل فرآیند پردازش

## 🔄 API Endpoints

- `GET /`: صفحه اصلی
- `POST /api/process_query`: پردازش سوال
- `GET /api/graph_info`: اطلاعات گراف
- `GET /api/sample_queries`: سوالات نمونه

## 📝 نکات مهم

1. **محیط مجازی**: همیشه قبل از اجرا محیط مجازی را فعال کنید
2. **مدل spaCy**: فقط یک بار نصب می‌شود
3. **پورت**: مطمئن شوید پورت 5000 آزاد است
4. **مرورگر**: از مرورگرهای مدرن استفاده کنید

## 🚀 اجرای سریع

```bash
# 1. فعال‌سازی محیط
.\graphrag_env\Scripts\activate

# 2. اجرای برنامه
python web_app.py

# 3. باز کردن مرورگر
# http://localhost:5000
```

حالا می‌توانید از رابط وب تعاملی GraphRAG استفاده کنید! 🎉 
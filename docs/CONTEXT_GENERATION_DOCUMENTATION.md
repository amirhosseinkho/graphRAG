# مستندات جامع تولید متن زمینه در سیستم GraphRAG

## فهرست مطالب
1. [معرفی کلی](#معرفی-کلی)
2. [معماری سیستم تولید متن زمینه](#معماری-سیستم-تولید-متن-زمینه)
3. [فایل‌های اصلی تولید متن زمینه](#فایل‌های-اصلی-تولید-متن-زمینه)
4. [انواع متن زمینه](#انواع-متن-زمینه)
5. [توابع اصلی تولید متن زمینه](#توابع-اصلی-تولید-متن-زمینه)
6. [فرآیندهای تولید متن زمینه](#فرآیندهای-تولید-متن-زمینه)
7. [روش‌های بهبود یافته](#روش‌های-بهبود-یافته)
8. [مدل‌های تولید متن](#مدل‌های-تولید-متن)
9. [مثال‌های کاربردی](#مثال‌های-کاربردی)
10. [نکات مهم و بهینه‌سازی](#نکات-مهم-و-بهینه‌سازی)

---

## معرفی کلی

سیستم GraphRAG از چندین روش مختلف برای تولید متن زمینه استفاده می‌کند که هر کدام برای کاربردهای خاصی طراحی شده‌اند. این سیستم قابلیت تولید متن زمینه هوشمند، علمی، بالینی و تخصصی را دارد.

### هدف اصلی
- تبدیل داده‌های گراف به متن قابل فهم برای مدل‌های زبان
- ارائه اطلاعات معنادار و ساختاریافته
- بهبود کیفیت پاسخ‌های سیستم
- پشتیبانی از انواع مختلف سوالات زیستی

---

## معماری سیستم تولید متن زمینه

### لایه‌های اصلی:
1. **لایه بازیابی (Retrieval Layer)**
   - استخراج نودها و یال‌های مرتبط
   - تحلیل نوع سوال
   - بازیابی هدفمند

2. **لایه غنی‌سازی (Enrichment Layer)**
   - تبدیل شناسه‌ها به نام‌های معنادار
   - اضافه کردن اطلاعات زیستی
   - تحلیل روابط

3. **لایه تولید متن (Generation Layer)**
   - انتخاب نوع متن زمینه
   - ساخت متن ساختاریافته
   - بهینه‌سازی خروجی

---

## فایل‌های اصلی تولید متن زمینه

### 1. `enhanced_context_generator.py`
فایل اصلی برای تولید متن زمینه بهبود یافته با قابلیت‌های پیشرفته.

**کلاس‌های اصلی:**
- `EnhancedContextGenerator`: کلاس اصلی تولید متن زمینه
- `EnhancedNode`: نمایش نود بهبود یافته
- `EnhancedEdge`: نمایش یال بهبود یافته

**توابع کلیدی:**
```python
def enhance_retrieval_result(self, retrieval_result: RetrievalResult) -> Dict[str, Any]
def create_enhanced_context_text(self, retrieval_result: RetrievalResult, context_type: str = "INTELLIGENT") -> str
```

### 2. `graphrag_service.py`
سرویس اصلی GraphRAG که شامل تمام توابع تولید متن زمینه است.

**توابع کلیدی:**
- `create_context_text()`: تابع اصلی تولید متن زمینه
- `_create_enhanced_context_text()`: تولید متن زمینه بهبود یافته
- `_create_intelligent_context_text()`: تولید متن زمینه هوشمند
- `_create_advanced_context_text()`: تولید متن زمینه پیشرفته

### 3. `node_lookup_system.py`
سیستم تبدیل شناسه‌ها به نام‌های معنادار.

**کلاس اصلی:**
- `NodeLookupSystem`: تبدیل شناسه‌ها به اطلاعات کامل نودها

---

## انواع متن زمینه

### 1. متن زمینه هوشمند (INTELLIGENT)
```python
def _create_intelligent_context(self, nodes, edges, query):
```
**ویژگی‌ها:**
- تحلیل عمیق روابط زیستی
- استنتاجات معنادار
- اهمیت بالینی
- کاربردهای عملی
- شناسایی ژن مرکزی
- تحلیل روابط معنادار

### 2. متن زمینه علمی-تحلیلی (SCIENTIFIC_ANALYTICAL)
```python
def _create_scientific_context(self, nodes, edges, query):
```
**ویژگی‌ها:**
- تحلیل آماری پیشرفته
- استنتاجات علمی
- تحلیل عمیق زیستی
- مناسب برای تحقیقات
- آمار بازیابی
- تحلیل نوع‌شناسی نودها

### 3. متن زمینه بالینی (CLINICAL_RELEVANCE)
```python
def _create_clinical_context(self, nodes, edges, query):
```
**ویژگی‌ها:**
- تمرکز بر روابط درمانی
- اهمیت پزشکی
- کاربردهای بالینی
- مدیریت بیماری‌ها
- تحلیل درمان‌ها
- اهمیت بالینی

### 4. متن زمینه مسیر زیستی (BIOLOGICAL_PATHWAY)
```python
def _create_pathway_context(self, nodes, edges, query):
```
**ویژگی‌ها:**
- تحلیل مسیرهای زیستی
- مکانیسم‌های سلولی
- نقاط کنترل
- درمان‌های هدفمند
- تحلیل مسیرها
- مکانیسم‌های زیستی

### 5. متن زمینه عمومی (GENERAL)
```python
def _create_general_context(self, nodes, edges, query):
```
**ویژگی‌ها:**
- سادگی و وضوح
- اطلاعات پایه
- مناسب برای کاربران عمومی
- ساختار ساده
- اطلاعات کلی

### 6. متن زمینه فشرده (COMPACT_DIRECT)
```python
def _create_compact_direct_context(self, retrieval_result: RetrievalResult) -> str:
```
**ویژگی‌ها:**
- متن فشرده و مستقیم
- اطلاعات ضروری
- مناسب برای مدل‌های سریع
- ساختار مختصر

### 7. متن زمینه روایی (NARRATIVE)
```python
def _create_narrative_context(self, retrieval_result: RetrievalResult) -> str:
```
**ویژگی‌ها:**
- متن روایی و توصیفی
- داستان‌سرایی زیستی
- مناسب برای آموزش
- جذابیت خوانایی

### 8. متن زمینه داده‌محور (DATA_DRIVEN)
```python
def _create_data_driven_context(self, retrieval_result: RetrievalResult) -> str:
```
**ویژگی‌ها:**
- نمایش روابط به صورت لیست
- داده‌های ساختاریافته
- مناسب برای تحلیل
- اطلاعات دقیق

### 9. متن زمینه گام به گام (STEP_BY_STEP)
```python
def _create_step_by_step_context(self, retrieval_result: RetrievalResult) -> str:
```
**ویژگی‌ها:**
- استدلال گام به گام
- توضیح قبل از سؤال
- مناسب برای آموزش
- منطق واضح

### 10. متن زمینه مکانیسمی تفصیلی (MECHANISTIC_DETAILED)
```python
def _create_mechanistic_detailed_context(self, retrieval_result: RetrievalResult) -> str:
```
**ویژگی‌ها:**
- تحلیل مکانیسمی تفصیلی
- جزئیات زیستی
- مناسب برای تحقیقات
- تحلیل عمیق

---

## توابع اصلی تولید متن زمینه

### 1. تابع اصلی `create_context_text()`
```python
def create_context_text(self, nodes: List[GraphNode], edges: List[GraphEdge], 
                       paths: List[List[str]]) -> str:
```
**عملکرد:**
- دریافت نودها، یال‌ها و مسیرها
- فراخوانی تابع بهبود یافته
- تولید متن زمینه نهایی

### 2. تابع بهبود یافته `_create_enhanced_context_text()`
```python
def _create_enhanced_context_text(self, retrieval_result: RetrievalResult) -> str:
```
**عملکرد:**
- تحلیل نوع سوال
- بازیابی هدفمند
- ایجاد متن ساختاریافته
- اضافه کردن آمار و تحلیل

### 3. تابع هوشمند `_create_intelligent_context_text()`
```python
def _create_intelligent_context_text(self, retrieval_result: RetrievalResult) -> str:
```
**عملکرد:**
- شناسایی ژن مرکزی
- تحلیل روابط معنادار
- استنتاجات زیستی
- اهمیت بالینی

### 4. تابع پیشرفته `_create_advanced_context_text()`
```python
def _create_advanced_context_text(self, retrieval_result: RetrievalResult) -> str:
```
**عملکرد:**
- تحلیل عمیق داده‌های گراف
- تحلیل آماری پیشرفته
- تحلیل نوع‌شناسی نودها
- تحلیل روابط معنادار

### 5. تابع غنی‌سازی داده‌ها `_enrich_retrieved_data()`
```python
def _enrich_retrieved_data(self, nodes: List[GraphNode], edges: List[GraphEdge], query: str) -> Dict[str, Any]:
```
**عملکرد:**
- استخراج اطلاعات بافت‌محور
- تحلیل عملکرد ژن‌ها
- ایجاد متن توصیفی زیستی
- غنی‌سازی روابط

### 6. تابع بازیابی هدفمند `_targeted_retrieval_for_question()`
```python
def _targeted_retrieval_for_question(self, query: str, intent: Dict) -> Dict[str, Any]:
```
**عملکرد:**
- تحلیل نوع سوال
- بازیابی هدفمند
- استخراج اطلاعات مرتبط
- ساخت داده‌های ساختاریافته

---

## فرآیندهای تولید متن زمینه

### فرآیند 1: تحلیل و غنی‌سازی داده‌ها

#### مرحله 1.1: استخراج اطلاعات بافت‌محور
```python
# یافتن بافت‌ها و ژن‌های مرتبط
anatomy_nodes = [n for n in nodes if n.kind == 'Anatomy']
gene_nodes = [n for n in nodes if n.kind == 'Gene']

for anatomy in anatomy_nodes:
    enriched_data['tissue_specific_info'][anatomy.name] = {
        'genes_expressed': [],
        'genes_upregulated': [],
        'genes_downregulated': [],
        'biological_significance': self._get_anatomy_significance(anatomy.name)
    }
```

#### مرحله 1.2: تحلیل عملکرد ژن‌ها
```python
for gene in gene_nodes:
    enriched_data['gene_functions'][gene.name] = {
        'biological_processes': [],
        'molecular_functions': [],
        'cellular_components': [],
        'pathways': [],
        'disease_associations': []
    }
```

### فرآیند 2: تولید متن زمینه بر اساس نوع

#### مرحله 2.1: انتخاب نوع متن زمینه
```python
if context_type == "INTELLIGENT":
    return self._create_intelligent_context(enhanced_nodes, enhanced_edges, query)
elif context_type == "SCIENTIFIC_ANALYTICAL":
    return self._create_scientific_context(enhanced_nodes, enhanced_edges, query)
elif context_type == "CLINICAL_RELEVANCE":
    return self._create_clinical_context(enhanced_nodes, enhanced_edges, query)
```

#### مرحله 2.2: ساخت متن زمینه هوشمند
```python
def _create_intelligent_context(self, nodes, edges, query):
    context_parts = []
    
    # 1. مقدمه
    context_parts.append(f"🧬 **متن زمینه هوشمند برای سوال:** {query}")
    
    # 2. نودهای کلیدی
    context_parts.append("📋 **نودهای کلیدی (با اطلاعات معنادار):**")
    for node in nodes:
        context_parts.append(f"• {node.name} ({node.kind})")
        if node.description:
            context_parts.append(f"  توضیح: {node.description}")
    
    # 3. روابط معنادار
    context_parts.append("🔗 **روابط معنادار:**")
    for edge in edges:
        context_parts.append(f"• {edge.relation_description}")
    
    # 4. تحلیل زیستی
    context_parts.append("🔬 **تحلیل زیستی و استنتاجات:**")
    
    return "\n".join(context_parts)
```

### فرآیند 3: بهبود و بهینه‌سازی

#### مرحله 3.1: تبدیل شناسه‌ها به نام‌های معنادار
```python
def enhance_retrieval_result(self, retrieval_result: RetrievalResult) -> Dict[str, Any]:
    # تبدیل نودها
    enhanced_nodes = []
    for node in retrieval_result.nodes:
        node_info = self.lookup_system.get_node_info(node.id)
        if node_info:
            enhanced_node = EnhancedNode(
                id=node.id,
                name=node_info.name,
                kind=node_info.kind,
                description=node_info.description,
                biological_role=node_info.biological_role,
                significance=node_info.significance,
                depth=node.depth,
                score=node.score
            )
            enhanced_nodes.append(enhanced_node)
```

#### مرحله 3.2: بهبود نمایش روابط
```python
# بهبود نمایش منبع و هدف
source_info = self.lookup_system.get_node_info(edge.source)
target_info = self.lookup_system.get_node_info(edge.target)

if source_info:
    source_display = f"{source_info.name} ({source_info.kind})"
    if source_info.description:
        source_display += f" - {source_info.description}"
```

---

## روش‌های بهبود یافته

### 1. سیستم NodeLookupSystem
```python
class NodeLookupSystem:
    def get_node_info(self, node_id: str) -> Optional[NodeInfo]:
        """دریافت اطلاعات کامل نود"""
        pass
```

**قابلیت‌ها:**
- تبدیل شناسه‌ها به نام‌های معنادار
- ارائه توضیحات زیستی
- نقش‌های زیستی
- اهمیت بالینی

### 2. بهبود نمایش روابط
```python
relation_descriptions = {
    "GpBP": "مشارکت در فرآیند زیستی",
    "GpPW": "مشارکت در مسیر زیستی",
    "GiG": "تعامل با ژن",
    "DaG": "ارتباط با ژن",
    "CtD": "درمان بیماری",
    "AeG": "بیان ژن در بافت"
}
```

### 3. تحلیل نوع سوال
```python
def analyze_question_intent(self, query: str) -> Dict[str, Any]:
    """تحلیل نوع سوال و استخراج موجودیت‌ها"""
    # شناسایی نوع سوال
    # استخراج موجودیت‌ها
    # تشخیص روابط هدف
    pass
```

### 4. بازیابی چندمرحله‌ای
```python
def multi_hop_search(self, query: str, max_depth: int = 3) -> List[Tuple[str, int, float, str, List[str]]]:
    """بازیابی چندمرحله‌ای با تحلیل الگوها"""
    pass
```

---

## مدل‌های تولید متن

### 1. مدل‌های محلی و رایگان
- `GENERAL_SIMPLE`: پاسخ ساده و عمومی
- `SIMPLE`: Simple Template
- `GPT_SIMULATION`: GPT Simulation
- `CUSTOM`: Custom Model
- `HUGGINGFACE`: HuggingFace Models

### 2. مدل‌های OpenAI
- `OPENAI_GPT_4O`: جدیدترین و قوی‌ترین
- `OPENAI_GPT_4O_MINI`: سریع و اقتصادی
- `OPENAI_GPT_4_TURBO`: تعادل سرعت و کیفیت
- `OPENAI_GPT_4`: کیفیت بالا
- `OPENAI_GPT_3_5_TURBO`: سریع و اقتصادی

### 3. مدل‌های Anthropic
- `ANTHROPIC_CLAUDE_3_5_SONNET`: جدیدترین
- `ANTHROPIC_CLAUDE_3_5_HAIKU`: سریع
- `ANTHROPIC_CLAUDE_3_OPUS`: قوی‌ترین
- `ANTHROPIC_CLAUDE_3_SONNET`: تعادل
- `ANTHROPIC_CLAUDE_3_HAIKU`: سریع

### 4. مدل‌های Google
- `GOOGLE_GEMINI_1_5_PRO`: جدیدترین
- `GOOGLE_GEMINI_1_5_FLASH`: سریع
- `GOOGLE_GEMINI_1_0_PRO`: پایدار
- `GOOGLE_GEMINI_1_0_FLASH`: سریع

### 5. سایر مدل‌های پیشرفته
- `META_LLAMA_3_1`: محلی
- `MISTRAL_AI`: کیفیت بالا
- `COHERE_COMMAND`: تخصصی
- `PERPLEXITY_SONAR`: جستجوگر

---

## مثال‌های کاربردی

### مثال 1: تولید متن زمینه هوشمند
```python
# ورودی
query = "What is the relationship between TP53 and cancer treatment?"

# فرآیند
enhanced_context = generator.create_enhanced_context_text(
    retrieval_result, 
    context_type="INTELLIGENT"
)

# خروجی
"""
🧬 متن زمینه هوشمند برای سوال: What is the relationship between TP53 and cancer treatment?

📋 نودهای کلیدی (با اطلاعات معنادار):
• TP53 (Gene)
  توضیح: سرکوب‌گر تومور و تنظیم‌کننده چرخه سلولی
  نقش زیستی: تنظیم آپوپتوز و ترمیم DNA

🔗 روابط معنادار:
• مشارکت در فرآیند زیستی
• درمان بیماری

🔬 تحلیل زیستی و استنتاجات:
• ژن‌های کلیدی و نقش‌های زیستی:
  - TP53: سرکوب‌گر تومور و تنظیم‌کننده چرخه سلولی

🧠 استنتاجات زیستی:
• روابط درمانی:
  - Carmustine برای درمان malignant glioma استفاده می‌شود

🏥 اهمیت بالینی:
بر اساس داده‌های ارائه شده، این روابط می‌تواند برای:
• درک مکانیسم‌های بیماری
• شناسایی اهداف درمانی
• توسعه داروهای جدید
مفید باشد.
"""
```

### مثال 2: تولید متن زمینه علمی
```python
# ورودی
query = "How does TP53 regulate apoptosis?"

# فرآیند
scientific_context = generator.create_enhanced_context_text(
    retrieval_result, 
    context_type="SCIENTIFIC_ANALYTICAL"
)

# خروجی
"""
🔬 تحلیل علمی-تحقیقاتی برای سوال: How does TP53 regulate apoptosis?

📊 آمار بازیابی:
• تعداد نودها: 4
• تعداد روابط: 3

🏷️ تحلیل نوع‌شناسی نودها:
• Gene: 1 نود (TP53)
• Biological Process: 1 نود (apoptosis)
• Disease: 1 نود (malignant glioma)
• Compound: 1 نود (Carmustine)

🔗 تحلیل روابط مولکولی:
• GpBP: 1 رابطه
• DaG: 1 رابطه
• CtD: 1 رابطه

🧬 تحلیل عمیق زیستی:
• تحلیل ژن‌های کلیدی:
  - TP53
    نقش زیستی: سرکوب‌گر تومور و تنظیم‌کننده چرخه سلولی
    توضیح: تنظیم‌کننده اصلی آپوپتوز

🔬 استنتاجات علمی:
بر اساس تحلیل داده‌ها:
• روابط ژن-بیماری شناسایی شد
• مسیرهای زیستی شناسایی شد
"""
```

### مثال 3: تولید متن زمینه بالینی
```python
# ورودی
query = "What treatments are available for breast cancer?"

# فرآیند
clinical_context = generator.create_enhanced_context_text(
    retrieval_result, 
    context_type="CLINICAL_RELEVANCE"
)

# خروجی
"""
🏥 متن زمینه بالینی برای سوال: What treatments are available for breast cancer?

📋 تحلیل بالینی:
• بیماری‌های مرتبط: breast cancer
• داروهای درمانی: Tamoxifen, Doxorubicin
• ژن‌های کلیدی: BRCA1, BRCA2

💊 درمان‌های موجود:
• Tamoxifen: مهارکننده گیرنده استروژن
• Doxorubicin: آنتی‌بیوتیک ضد تومور

🔬 اهمیت بالینی:
• تشخیص زودهنگام
• درمان هدفمند
• پیگیری بیماران

📊 آمار درمانی:
• تعداد داروهای مرتبط: 2
• تعداد ژن‌های مرتبط: 2
"""
```

---

## نکات مهم و بهینه‌سازی

### 1. بهینه‌سازی عملکرد
- استفاده از کش برای اطلاعات نودها
- محدود کردن تعداد نودها و یال‌ها
- بهینه‌سازی جستجو
- استفاده از الگوریتم‌های کارآمد

### 2. کیفیت متن زمینه
- بررسی صحت اطلاعات
- اعتبارسنجی روابط
- بهبود خوانایی
- کنترل کیفیت خروجی

### 3. قابلیت توسعه
- اضافه کردن انواع جدید متن زمینه
- بهبود تحلیل‌ها
- اضافه کردن قابلیت‌های جدید
- پشتیبانی از مدل‌های جدید

### 4. مستندسازی
- نگهداری مستندات به‌روز
- مثال‌های کاربردی
- راهنمای استفاده
- مستندات API

### 5. مدیریت خطا
- کنترل خطاهای API
- مدیریت محدودیت‌های مدل
- سیستم fallback
- لاگ‌گیری مناسب

---

## خلاصه

سیستم تولید متن زمینه GraphRAG شامل:

1. **فایل‌های اصلی:**
   - `enhanced_context_generator.py`
   - `graphrag_service.py`
   - `node_lookup_system.py`

2. **انواع متن زمینه:**
   - هوشمند (INTELLIGENT)
   - علمی-تحلیلی (SCIENTIFIC_ANALYTICAL)
   - بالینی (CLINICAL_RELEVANCE)
   - مسیر زیستی (BIOLOGICAL_PATHWAY)
   - عمومی (GENERAL)
   - فشرده (COMPACT_DIRECT)
   - روایی (NARRATIVE)
   - داده‌محور (DATA_DRIVEN)
   - گام به گام (STEP_BY_STEP)
   - مکانیسمی تفصیلی (MECHANISTIC_DETAILED)

3. **توابع کلیدی:**
   - `create_context_text()`
   - `_create_enhanced_context_text()`
   - `_create_intelligent_context_text()`
   - `_create_advanced_context_text()`
   - `_enrich_retrieved_data()`
   - `_targeted_retrieval_for_question()`

4. **فرآیندهای اصلی:**
   - تحلیل و غنی‌سازی داده‌ها
   - تولید متن زمینه بر اساس نوع
   - بهبود و بهینه‌سازی
   - بازیابی چندمرحله‌ای

5. **روش‌های بهبود یافته:**
   - سیستم NodeLookupSystem
   - بهبود نمایش روابط
   - تحلیل نوع سوال
   - بازیابی چندمرحله‌ای

6. **مدل‌های تولید متن:**
   - مدل‌های محلی و رایگان
   - مدل‌های OpenAI
   - مدل‌های Anthropic
   - مدل‌های Google
   - سایر مدل‌های پیشرفته

این سیستم قابلیت تولید متن زمینه با کیفیت بالا و مناسب برای کاربردهای مختلف را دارد و از تکنیک‌های پیشرفته برای بهبود کیفیت و کارایی استفاده می‌کند. 